apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: vetfrank/vet
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
